//sum unwrapping

typedef Test = union Int!inf | Unit!inf

func testSum_0(x: Test!2) -> Int!1 {
	if (case let left = inl x) {
		assertTypeEqual(x, Test!1)
		assertTypeEqual(left, Int!inf)
		return left
	}
	else {
		return 10
	}
}
