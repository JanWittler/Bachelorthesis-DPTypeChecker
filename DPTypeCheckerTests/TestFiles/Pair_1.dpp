//pair with different arguments
func testPair_1(x: Int!4, y: Unit!1) -> (Int!1, Unit!1)!1 {
    let pair = (x, y)

    assertTypeEqual(x, Int!3)
    assertTypeEqual(y, Unit!0)
    assertTypeEqual(pair, (Int!1, Unit!1)!1)

    return pair
}
